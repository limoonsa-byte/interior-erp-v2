========================================
전체 복사 최종 완성본 사용법
========================================

1. "doGet-수정본.txt" 파일을 엽니다.
   → 이제 include 함수가 포함되어 있습니다!

2. 전체 내용 복사 (Ctrl+A, Ctrl+C)

3. Apps Script의 code.gs에서:
   - 기존 doGet 함수 전체 삭제
   - 붙여넣기

4. "getDrawingListForErp_-추가할함수.txt" 파일을 엽니다.

5. 전체 내용 복사 (Ctrl+A, Ctrl+C)

6. code.gs 파일의 맨 마지막 (setupPriceTables 함수 뒤)에 붙여넣기

7. 저장 → 배포 → 웹 앱 → 버전 수정

========================================
완료!
========================================

이제 다음이 모두 포함됩니다:
✅ doGet 함수 (맨 위에 ?action=list API 추가)
✅ include 함수 (HTML 템플릿용)
✅ getDrawingListForErp_() 함수 (ERP API용)
✅ === 를 == 로 변경 완료
