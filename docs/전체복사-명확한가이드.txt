========================================
전체 복사 명확한 가이드
========================================

다음 2개 파일을 사용하세요:

1. "doGet-수정본.txt" 
   → 이 파일 전체를 복사해서 사용

2. "getDrawingListForErp_-추가할함수.txt"
   → 이 파일 전체를 복사해서 사용

========================================
사용 방법:
========================================

1단계: 기존 code.gs 파일을 엽니다 (Apps Script)

2단계: "doGet-수정본.txt" 파일을 엽니다
   → 전체 내용 복사 (Ctrl+A, Ctrl+C)
   → code.gs에서 기존 doGet 함수 전체를 찾아서 삭제
   → 붙여넣기

3단계: "getDrawingListForErp_-추가할함수.txt" 파일을 엽니다
   → 전체 내용 복사 (Ctrl+A, Ctrl+C)
   → code.gs 파일의 맨 마지막 (setupPriceTables 함수 뒤)에 붙여넣기

4단계: 저장 → 배포 → 웹 앱 → 버전 수정

========================================
완료!
========================================

이제 다음이 모두 포함됩니다:
✅ doGet 함수 (맨 위에 ?action=list API 추가)
✅ include 함수 (HTML 템플릿용)
✅ getDrawingListForErp_() 함수 (ERP API용)
✅ === 를 == 로 변경 완료
